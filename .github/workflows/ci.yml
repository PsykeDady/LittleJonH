name: ci

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: '8'
        architecture: x64
    - name: Test
      run: ./mill -i --disable-ticker __.test
